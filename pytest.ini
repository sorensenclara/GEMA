[pytest]
DJANGO_SETTINGS_MODULE = gema.settings
python_files = tests.py test_*.py *_tests.py
addopts = --import-mode=importlib
markers =
    e2e: end-to-end browser test via Playwright (slow, needs a live server)
